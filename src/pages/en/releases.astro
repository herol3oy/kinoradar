---
import { env } from "cloudflare:workers";
import HomePage from "../../layouts/HomePage.astro";
import { loadInitialReleasePage } from "../../server/release-page";

const releaseCalendar = await loadInitialReleasePage(env.SHOWTIMES, env, "en", Astro.url);
---

<HomePage locale="en" releaseCalendar={releaseCalendar} />
